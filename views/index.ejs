<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ('./partials/head.ejs') %>
</head>

<body>
    <div class="headline">
        <h1>Product Inventory Page</h1>
        <button><a href="/products/new" class="btn btn-secondary">Add New Item</a></button>
    </div>
    <div class="container">
        <section>

            <% for(let i = 0; i < allProducts.length; i++) { %>

            <div class="card" style="width: 18rem;">
                <a href="/products/<%= i%>">
                <img src="<%= allProducts[i].img%>" class="card-img-top" alt="Product Image" width="150">
                </a>
                <div class="card-body">
                    <h5 class="card-title"><%= allProducts[i].name%></h5>
                    <p>Price: <%= allProducts[i].price%></p>
                </div>
            </div>
            <br>
            <% } %>

        </section>
    </div>
    <%= console.log(allProducts)%>
</body>

</html>